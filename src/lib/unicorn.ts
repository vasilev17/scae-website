/**
 * Published Unicorn Studio exhibit overlay (beam + hover interior).
 * Public embed id, not a secret. Bump `update` after each Unicorn republish
 * so browsers and the Unicorn CDN drop the previous scene payload.
 */
export const UNICORN_EXHIBIT_ID = 'ZXfqMTYyhnaXQ5sFVsAu';
